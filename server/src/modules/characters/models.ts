export class Character {
  name: string;
  skill: string;
  img: string;

  constructor(name: string, skill: string, img: string) {
    this.name = name;
    this.skill = skill;
    this.img = img;
  }
}